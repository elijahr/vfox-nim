# vfox & mise tool versions
# Lua tooling is provisioned by .github/actions/setup-lua (hererocks PUC Lua 5.1)
# and by ~/.vfox-nim-rocks locally — NOT by mise, to avoid a slow, readline-fragile
# source build. stylua + actionlint are pinned in mise.toml.
